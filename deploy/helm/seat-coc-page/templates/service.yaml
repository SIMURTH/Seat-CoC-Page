apiVersion: v1
kind: Service
metadata:
  name: {{ include "seat-coc-page.fullname" . }}
  labels:
    {{- include "seat-coc-page.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seat-coc-page.selectorLabels" . | nindent 4 }}
